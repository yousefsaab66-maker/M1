/** @deprecated Import from `@/lib/storefront-query` — kept for older imports. */
export {
  fetchStorefront as fetchSiteContent,
  upsertSiteContent,
  upsertCollectionsContent,
  upsertStorefront,
  fetchStorefront,
} from "@/lib/storefront-query";

export type { FetchStorefrontResult as FetchSiteContentResult } from "@/lib/storefront-query";
